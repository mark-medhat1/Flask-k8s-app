apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: flask-service
  name: flask-service
spec:
  ports:
  - name: 5000-5000
    port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: flask-app
  type: NodePort
status:
  loadBalancer: {}
